import type{MetadataRoute}from"next";import{products}from"@/lib/data";import{locales}from"@/lib/i18n";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"https://b2b.oukitel.com";const localized=locales.flatMap(locale=>[`/${locale}`,`/${locale}/contact`]);const paths=["","/products","/portable-power-stations","/solar-generators","/solar-panels","/portable-power-station-wholesale","/distributor-program","/oem-portable-power-station","/factory","/applications","/about","/resources","/downloads","/contact",...localized,...products.map(p=>`/products/${p.slug}`)];return paths.map(url=>({url:base+url,lastModified:new Date(),changeFrequency:url.startsWith("/products")?"monthly":"weekly",priority:url===""?1:.8}))}
